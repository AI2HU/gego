# Legacy SQLite migrations (frozen)

Do not add or modify migrations in this directory.

PostgreSQL migrations live in `../postgres/`. SQLite migrations exist only for legacy deployments that have not upgraded yet.

See `internal/db/README.md`.
